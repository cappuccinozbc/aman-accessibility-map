# 🚀 aman-accessibility-map 工作流程

## 📂 项目结构

```
aman-accessibility-map/
├── index.html          # ⚠️ 版本导航页面（不要修改，除非添加新版本）
├── v1/index.html       # 📍 v1 基础版本（稳定，仅维护）
├── v2/index.html       # ✨ v2 最新版本（当前开发主分支）
└── ...
```

---

## 🎯 后续工作流程

### 开发新功能时

**1. 切换到工作目录：**
```bash
cd /root/.openclaw/workspace/aman-accessibility-map
```

**2. 确认当前分支：**
```bash
git branch --show-current
# 应该显示：gh-pages
```

**3. 修改 v2 版本：**
```bash
# 直接编辑 v2/index.html
# 或者先查看当前版本
cat v2/index.html
```

**4. 测试修改：**
打开浏览器访问：
https://cappuccinozbc.github.io/aman-accessibility-map/v2/

**5. 提交并推送：**
```bash
git add v2/index.html
git commit -m "Update v2: 描述你的修改"
git push origin gh-pages
```

**6. 等待部署完成（约 30-60 秒）**
刷新浏览器查看效果。

---

## 🔄 版本发布流程

当 v2 积累足够功能后，可以发布 v3：

**1. 创建 v3 目录：**
```bash
cp -r v2/ v3/
```

**2. 在 v3/index.html 中开发新功能**

**3. 修改版本导航页面 index.html：**
- 添加 v3 版本链接
- 更新"最新"标签

**4. 提交并推送：**
```bash
git add v3/ index.html
git commit -m "Release v3: 新版本说明"
git push origin gh-pages
```

---

## ⚠️ 重要注意事项 ✚

**✅ DO（应该做）：**
- 后续修改在 `v2/index.html` 中进行
- 每次修改后提交到 gh-pages 分支
- v2 是当前主开发版本
- 提交信息使用 "Update v2: " 前缀

**❌ DON'T（不要做）：**
- 不要修改根目录的 `index.html`（除非发布新版本）
- 不要修改 `v1/index.html`（v1 是稳定版本）
- 不要在 `main` 分支工作（开发在 gh-pages 分支）

---

## 🔗 快速访问

- **版本导航：** https://cappuccinozbc.github.io/aman-accessibility-map/
- **v2（当前开发）：** https://cappuccinozbc.github.io/aman-accessibility-map/v2/
- **v1（稳定版）：** https://cappuccinozbc.github.io/aman-accessibility-map/v1/

---

## 📝 当前状态

| 版本 | 状态 | 功能 |
|------|------|------|
| v1 | ✅ 稳定 | 基础可达区域搜索 |
| v2 | 🔥 开发中 | 导航路径可视化 |
| 导航页 | ✅ 活跃 | 版本选择入口 |

**当前工作分支：** `gh-pages`  
**当前开发版本：** `v2/`

---

## 🆘 常见问题

**Q: 如何查看当前修改了哪些文件？**
```bash
git status
```

**Q: 如何撤销未提交的修改？**
```bash
git checkout v2/index.html
```

**Q: 如何查看修改历史？**
```bash
git log --oneline -10 v2/index.html
```

**Q: 如何比较 v1 和 v2 的差异？**
```bash
diff v1/index.html v2/index.html
```

---

## ✨ 下一步

准备好开发了！直接修改 `v2/index.html`，然后按上述流程提交即可。
